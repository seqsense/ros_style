name: ci

on:
  pull_request:
    branches:
      - master

jobs:
  python-pass:
    uses: ./.github/workflows/pylint.yml
    with:
      style-branch: ${{ github.head_ref }}
      files: ./test/pass.py
